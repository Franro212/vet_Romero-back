import { Schema, model } from "mongoose";

const AdminSchema = Schema({
  email: {
    type: String,
    required: true,
    unique: true,
    match: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
  },
  password: {
    type: String,
    required: true,
  },
});

export default model("Admin", AdminSchema);
